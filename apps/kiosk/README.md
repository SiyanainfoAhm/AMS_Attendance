## AMS Kiosk (Flutter) — ON HOLD

This app is intentionally not implemented yet per current instruction.

When resumed (Phase 6), it will support:
- kiosk activation / device registration
- face-based attendance capture
- offline queue + auto sync
- station mapping + audit buzzer/alerts

