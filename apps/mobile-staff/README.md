## AMS Staff Mobile (Flutter)

Phase 1 placeholder.

### Notes
- This app will use **custom AMS auth** (no Supabase Auth).
- It will call Edge Function APIs for all business actions.

### Next (Phase 5)
- Create Flutter project (`flutter create`) and wire:
  - login + company selection
  - geo-fenced attendance
  - attendance history

